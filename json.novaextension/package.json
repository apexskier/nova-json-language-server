{
  "name": "json.novaextension",
  "private": true,
  "dependencies": {
    "vscode-json-languageserver": "^1.3.1"
  },
  "scripts": {
    "update": "npm update && npm shrinkwrap && rm -rf node_modules"
  }
}
